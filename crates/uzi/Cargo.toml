[package]
name = "personal-taoli-uzi"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Rust port of the UZI-Skill analysis core (scoring / rules / synthesis / render)"
publish = false

[dependencies]
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
