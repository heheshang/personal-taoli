# C-06 主备fencing：任务文档

- 状态：`in_progress`；主备fencing模块已实现
- 上游：A-05 行情归档与连续影子统计、B-04 账务对账与控制面
- 安全边界：仅隔离环境验证，无真实或测试网订单副作用

## 1. 任务清单

| 阶段 | 任务 | 产出 | 状态 |
|---|---|---|---|
| 需求 | 定义节点管理、令牌管理和故障检测需求 | 需求文档 | completed |
| 分析 | 核对现有主备切换需求和故障场景 | 设计约束 | completed |
| 设计 | 定义fencing管理器和令牌结构 | 设计文档 | completed |
| 实现 | 实现主备fencing核心模块 | `fencing.rs` | completed |
| 实现 | 集成到lib.rs | `lib.rs` | completed |
| 验证 | 运行测试并验证结果 | 测试报告 | in_progress |
| 发布 | 同步架构和AIDLC文档 | 发布记录 | pending |

## 2. 必须通过的行为

1. 节点注册必须支持角色和状态管理。
2. 令牌生成必须支持过期时间配置。
3. 令牌验证必须支持过期检查。
4. 主备切换必须验证新主节点状态。
5. 心跳检测必须支持超时配置。
6. 测试套件必须覆盖所有预定义测试场景。

## 3. 验证证据

- `cargo fmt --all -- --check`：通过。
- `cargo test --workspace`：68 项通过。
- `cargo clippy --workspace --all-targets -- -D warnings`：通过，无警告。
- `npm run build`：通过，Vue 类型检查和 Vite 生产构建完成。
- 主备fencing测试套件运行：所有测试通过。

上述证据只证明主备fencing模块的功能正确性，不证明真实主备切换场景下的系统行为。

## 4. 发布阻塞

- 在生产环境执行主备切换测试。
- 修改生产环境配置。
- 影响系统正常运行。
- 将测试结果解释为真实主备切换安全证明。

## 5. 发布后下一唯一入口

继续阶段C其他任务的开发。主备fencing模块完成后，继续实现运行手册演练。
