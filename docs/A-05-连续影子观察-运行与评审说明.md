# A-05 连续影子观察：运行与评审说明

## 1. 目的与当前状态

本文用于启动、监督、停止、回放和评审 A-05 正式连续观察窗口。它不是实盘运行手册；程序没有下单能力。

- 状态：已于 2026-09-08T10:30:41Z 按所有者决定延期并优雅停止；A-05 尚未发布。
- 已停止窗口起点：2026-09-08T09:52:14Z。
- 已停止窗口终点：2026-09-08T10:30:40Z；仅运行约 38 分 26 秒，不满足连续 14 天要求。
- 原最早 14 天评审时间：2026-09-22T09:52:14Z，因窗口提前停止而失效。
- 保留归档：`data/archive/a05-shadow-20260908.ndjson`，只作为实现和短时运行证据。
- 缺口日志：`data/archive/a05-shadow-20260908.ndjson.gaps.ndjson`。
- 运行 ID：`1788861134042-72723-1`。
- 原受管进程名：`taoli-shadow-a05`；已退出，退出码 0，监督器重启数 0。

未来恢复 A-05 时必须使用新归档文件、新起点和新 14 天窗口；不得把本次短窗口与后续窗口拼接。首末事件相差 14 天也不等于进程连续在线 14 天，仍必须按第 6 节核对停机和相邻事件间隔。

## 2. 前置条件

1. 工作目录为仓库根目录。
2. `config/observer.toml` 中双方资格确认保持 `false`，除非所有者已单独提供并批准真实只读账户验证；不得配置交易或提现权限。
3. `data/` 位于 `.gitignore`，磁盘容量足够保存高频 NDJSON。
4. 质量门禁通过：

```bash
cargo fmt --all -- --check
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
cargo build --workspace --release
```

5. 正式窗口必须使用独立文件。不得把预检 `data/archive/observations.ndjson` 作为正式证据。

## 3. 启动

在具备进程监督和失败自动重启的环境中运行桌面进程（配置取 `config/observer.toml`，环境变量驱动自动连续观察）：

```bash
TAOLI_OBSERVER_AUTOSTART=1 \
TAOLI_OBSERVER_ARCHIVE=data/archive/a05-shadow-20260908.ndjson \
./target/release/personal-taoli
```

启动成功的 stderr 必须依次出现：

```text
continuous observation autostart requested
continuous observation running (read-only) archive=data/archive/a05-shadow-20260908.ndjson
```

监督器必须保留启动时间、退出码、信号和重启次数。当前工作站由受管进程 `taoli-shadow-a05` 监督桌面进程；故障退出自动重启。不要用未经监督的后台 shell 进程代替正式窗口。

## 4. 日常在线核对

只读在线回放不会阻塞写入器：在桌面控制台（CONTROL DECK）操作 `REPLAY`（归档路径取正式窗口文件），或最终评审期使用只读脚本按同一语义读取命令/脚本启动瞬间的完整文件长度。每次核对至少保存或记录：

- `archive_bytes` 和 `records` 持续增长；
- `first_event_at_ms` 不变化，`last_event_at_ms` 前进；
- `replayed_without_mismatch=true`；
- `gap_records`、`dropped_events`；
- `ignored_incomplete_tail_bytes`、`ignored_gap_tail_bytes`；
- `health_records`、`invalid_duration_ms`、双方 `reconnects`；
- 方向评估数、正净收益数、准入数、收益/容量分布、拒绝类别和尾部样本；
- 监督器的 PID、uptime、退出/重启记录。

在线回放偶尔出现非零 `ignored_incomplete_tail_bytes` 可仅表示末行正在写入；下一次回放应回到 0。持续非零、增长或重启修复记录必须调查。

## 5. 停止与恢复

### 5.1 计划停止

向受监督的进程发送 SIGTERM 或 Ctrl-C。程序应输出：

```text
observer stopped; flushing archive
```

退出码必须为 0。停止后立即执行回放并确认：

- `replayed_without_mismatch=true`；
- `ignored_incomplete_tail_bytes=0`；
- `ignored_gap_tail_bytes=0`；
- 没有新增未解释缺口。

### 5.2 非计划退出

1. 先保留监督器退出原因和时间。
2. 自动重启后确认新记录使用新的 `run_id`，新运行的 `ordinal` 从 1 开始。
3. 执行回放。若启动修复了无换行尾部，缺口日志应有 `discarded ... incomplete bytes`，且 `dropped_events=0`。
4. 计算退出前最后事件到重启后首个事件的间隔，并记录为进程停机；不能计入连续在线时间。
5. 若发现完整换行损坏行，不要修改原始文件。复制保全后停止将该窗口作为完整证据，并调查磁盘、并发写入和进程版本。

### 5.3 写入故障

`archive write failed`、`archive gap write failed` 或 `archive submit failed` 均为 P0 观察证据故障。行情扫描继续不代表窗口有效。修复磁盘/权限后执行回放，量化缺口并决定重启新窗口或明确拒绝 A05-R06。

## 6. 14 天连续性核对

最终评审不能只读取 `observed_duration_ms`。按以下顺序核对：

1. **时间边界**：首条事件不早于正式起点；末条事件不早于 2026-09-22T09:52:14Z。
2. **原始顺序**：回放成功，所有运行内序号严格递增，事件时间无倒退。
3. **运行边界**：列出归档中所有 `run_id` 及各自首末时间、首末序号；与监督器重启记录逐一对应。
4. **采样连续性**：配置周期为 2 秒。计算相邻记录时间差；任何显著超过 `poll_interval_ms` 的间隔都要与行情健康事件、账户刷新、进程停止或主机休眠对应。不得把无事件停机误算为在线。
5. **主机/监督器连续性**：核对 uptime、退出码、重启次数和宿主机睡眠/重启。不能解释的停机使“连续 14 天”门禁失败。
6. **数据完整性**：归档和缺口日志均无未完成尾部；`gap_records=0`、`dropped_events=0`。若非零，逐条调查并说明是否必须重开窗口。
7. **行情恢复样本**：Binance 与 Bybit 的自然重连计数各至少增加 1，且至少有一次 `HealthEvent` 后恢复为双方 `VALID` 的决策事件；主动重连烟测（桌面 RECONNECT SMOKE）不计入。
8. **独立机会样本**：按归档顺序分别跟踪两个方向。`expected_net_profit > 0` 从非正值转入，或与同方向上一条正值样本相隔至少 30 秒时，新计一个独立正净收益候选事件段；持续正值段只计一次。两个方向合计至少 100 个并分别报告，不能用方向评估数、连续采样数或 `accepted_opportunities` 替代。

当前实现不会直接输出最大相邻事件间隔或运行清单。最终评审可使用只读脚本分析 NDJSON，但脚本、命令、输出摘要和判定必须随评审记录保存；不得改写原始归档。

## 7. A-05 发布检查表

- [ ] 已达到最早评审时间且完整覆盖 14 天。
- [ ] 格式、38 项测试和严格 Clippy 仍通过；若数量变化，记录实际结果。
- [ ] 正式归档固定长度回放一致。
- [ ] 所有运行 ID、退出、重启和宿主机事件可解释。
- [ ] 相邻采样间隔已核对，连续在线结论不依赖首末事件跨度。
- [ ] 缺口、丢弃和未完成尾部为 0，或所有非零项已有不影响结论的书面证据。
- [ ] Binance 与 Bybit 各至少一次自然重连，且至少一次真实 `非 VALID → VALID` 恢复；主动烟测未计入。
- [ ] 两个方向合计至少 100 个独立正净收益候选事件段并分方向报告；未临时缩短 30 秒间隔或放宽收益、费率、新鲜度、资格和准入限制。
- [ ] 收益、容量、拒绝类别和最差尾部样本已评审。
- [ ] 无真实账户凭证时，报告明确说明账户资格、只读权限和实际费率仍未实测。
- [ ] 架构文档当前状态、AIDLC 追踪矩阵、发布记录和剩余风险已更新。
- [ ] 所有者明确决定 `released`、继续观察、重开窗口或 `rejected`；没有“基本完成”状态。

## 8. 当前已验证证据

- `cargo test --workspace`：38 项通过。
- `cargo clippy --workspace --all-targets -- -D warnings`：通过，无警告。
- 受控 SIGTERM：退出码 0；36 条归档记录精确回放，未完成尾字节为 0。
- 正式窗口首批在线回放：15 条决策、30 个方向、缺口 0、未完成尾字节 0、`replayed_without_mismatch=true`。

这些证据只证明实现和窗口启动正确，不证明 A05-R06 已完成，不证明策略盈利，也不授权进入 B-01 或实盘。

## 9. 中期检查记录

### 2026-09-08T10:26:23Z

- 监督器：`taoli-shadow-a05` 为 `ready`，PID `72723`，`restart=on-failure`，检查时 uptime 33 分 42 秒，监督器重启数为 0；命令和正式归档路径与第 3 节一致。
- 固定长度回放：1,011 条决策、2,022 个方向，`replayed_without_mismatch=true`；`gap_records=0`、`dropped_events=0`、两个未完成尾字节字段均为 0。
- 连续性只读分析：随后固定 15,500,316 字节分析 1,051 条完整记录；单一 `run_id=1788861134042-72723-1`，序号严格覆盖 1–1,051，首末时间为 2026-09-08T09:52:14.043Z 至 2026-09-08T10:27:14.084Z，最大相邻记录间隔 2,006 ms；当次未发现无事件停机。
- 行情恢复：Bybit 最大 `generation=2`、`reconnects=1`；Binance 最大 `generation=1`、`reconnects=0`。归档尚无 `HealthEvent`，因此双方各一次自然重连和一次 `非 VALID → VALID` 恢复门槛未完成。
- 机会样本：`positive_net_opportunities=0`，按 30 秒规则重新统计的独立正净收益候选事件段为两个方向各 0，合计 0/100；`accepted_opportunities=0`。
- 经济分布：当次回放 `expected_net_profit` 最小值 `-0.18849201000000`、P50 `-0.16542870000000`、P95 `-0.16018520100000`、最大值 `-0.14239961100000`；当前样本不支持正净收益结论。
- 判定：A-05 保持 `in_progress`。当时正式窗口继续运行；不得进入 B-01，不得降低成本、准入或独立事件段门槛。

### 2026-09-08T10:30:41Z 延期与停止

- 所有者决定把完整连续观察留到后期，先进入代码开发；该决定改变开发顺序，不豁免 A05-R06 或任何上线门槛。
- 监督器对 `taoli-shadow-a05` 执行优雅停止：退出码 0、总运行 38 分 27 秒、重启数 0。
- 停止后固定长度回放：1,154 条决策、2,308 个方向、0 条健康事件，观察跨度 2,306,057 ms；Bybit 自然重连 1 次、Binance 0 次，正净收益与准入机会均为 0。
- 完整性：`gap_records=0`、`dropped_events=0`、两个未完成尾字节字段均为 0，`replayed_without_mismatch=true`。
- 该窗口明确作废为 A05-R06 连续性验收窗口，但保留为实现、优雅停止和短时归档证据。恢复评审时必须新开完整窗口，不能和本窗口累计时长或拼接样本。
- 开发顺序例外：允许开始 B-01 的 PAPER 安全核心；阶段 A、实盘和任何真实下单权限仍被 A-05 阻塞。

### 2026-09-09T00:19Z 结构重构完成后重新武装（事实记录，语义判定留待最终评审）

- 背景：结构重构（三 crate 工作区、独立 CLI 二进制形态）完成，质量门禁通过；按任务文档受管命令重新武装 `taoli-shadow-a05`，沿用同一归档 `data/archive/a05-shadow-20260908.ndjson` 与缺口日志。
- 新运行 `1788913140512-42406-1`：监督器 PID `42406`，首个归档记录在行 1155（2026-09-09T00:19:01.097Z），共写 80 条（行 1155–1234），末条 2026-09-09T00:21:39.100Z。
- 自动重启验证：2026-09-09T00:21:42Z 由操作者对该 PID 执行 `SIGKILL`（崩溃模拟，非自然故障）；监督器 `restart=on-failure` 自动拉起，重启数 1，新 PID `44814`。
- 新运行 `1788913302263-44814-1`：首个归档记录在行 1235（2026-09-09T00:21:42.907Z），当前运行中；到 2026-09-09T00:24:34.909Z 已写 87 条。上一运行末条到本运行首条的停机间隔为 3,807 ms，按 §5.2.4 记录为进程停机，不计入连续在线时间。
- 完整性核对（写者在线的固定长度回放，行 1257 与行 1321 两个时刻）：`replayed_without_mismatch=true`，`gap_records=0`、`dropped_events=0`、`ignored_incomplete_tail_bytes=0`、`ignored_gap_tail_bytes=0`；归档尾部字节为完整换行，无重启修复丢弃。
- 归档现状（截至 2026-09-09T00:24:35Z）：三个运行 ID 共存同一文件——`1788861134042-72723-1`（行 1–1154，前导 38 分短窗）、`1788913140512-42406-1`（行 1155–1234）、`1788913302263-44814-1`（行 1235 起，运行中）；各运行内序号连续、首行 `ordinal=1`。
- 待评审裁决点（按所有者决定保持现状，仅记录）：本节第 1 条与 10:30:41 记录的「作废、恢复须新归档、不得拼接」表述，和任务文档/AIDLC/架构文档「同一归档持续运行、最早 2026-09-22T09:52:14Z 评审、pending（运行中）」记录不一致；且按 §6 连续在线口径，新进程自 2026-09-09T00:19Z 起，到 2026-09-22T09:52:14Z 在线仅约 13.2 天，未达 14 天。前导短窗（`…-72723-1`、`…-42406-1`）能否计入、以及最早可评审日期（09-22 基准或 09-23T00:19Z 之后），由最终评审按 §6 逐项裁决，本记录不预作判定；§1 状态行未随之改写。

### 2026-09-09T00:43Z 纯 Tauri 收敛后桌面进程接管（事实记录，语义判定留待最终评审）

- 背景：按 C-01 决策把仓库收敛为纯 Rust + Tauri 桌面形态（工作区仅 `crates/core` 与 `src-tauri`，独立 CLI 成员整体删除），四道质量门禁通过（fmt、DB 测试 48 项、clippy `-D warnings`、release 构建）。
- 接管前桌面烟测（scratch 归档，不触碰正式证据）：`TAOLI_OBSERVER_AUTOSTART=1` 启动 `./target/release/personal-taoli`，自动连续观察 14 秒写入 7 条决策记录后 SIGTERM，进程 `exit=0`、归档无未完成尾部；scratch 文件随后删除，其引入的烟测 run 前缀不入正式归档。
- 旧 CLI 监督进程（PID `44814`）优雅停止 `exit=0`，末条记录行 1883（2026-09-09T00:43:18.927Z）；受管命令改为桌面进程 + 环境变量：`TAOLI_OBSERVER_AUTOSTART=1 TAOLI_OBSERVER_ARCHIVE=data/archive/a05-shadow-20260908.ndjson ./target/release/personal-taoli`（cwd 仓库根）。
- 新运行 `1788914607692-64268-1`：监督器 PID `64268`，首个归档记录在行 1884（2026-09-09T00:43:28.318Z）；上一运行末条到本运行首条的停机间隔 9,391 ms，按 §5.2.4 记录为进程停机，不计入连续在线时间。
- 桌面会话与旧 CLI 同为只读、无订单入口：连续观察经环境变量自动开始，配置仍取 `config/observer.toml`；SIGTERM/关窗路径先冲刷归档再退出。
- 完整性核对（截至 2026-09-09T00:44:30Z，行 1915）：`gap_records=0`、`dropped_events=0`、缺口日志 0 行、无重启修复丢弃。