# E-05 性能指标收集：设计文档

文档状态：`released`（所有者 2026-09-09 发布签收）。实现性能指标收集模块。

## 1. 模块边界

- `crates/core/src/performance.rs`：性能指标收集核心模块，包含指标收集、分析和统计。

## 2. 性能指标类型

| 类型 | 描述 | 使用场景 |
|---|---|---|
| ResponseTime | 响应时间 | API响应时间 |
| Throughput | 吞吐量 | 系统吞吐量 |
| ErrorRate | 错误率 | 系统错误率 |
| Latency | 延迟 | 系统延迟 |
| ResourceUsage | 资源使用率 | 系统资源使用 |

## 3. 性能指标收集器设计

```text
PerformanceMetricsCollector {
    metrics: Arc<RwLock<Vec<PerformanceMetric>>>,
}

impl PerformanceMetricsCollector {
    pub fn new() -> Self;
    pub async fn record_metric(&self, name: &str, metric_type: PerformanceMetricType, value: f64, unit: &str, labels: HashMap<String, String>);
    pub async fn get_metrics(&self) -> Vec<PerformanceMetric>;
    pub async fn get_metrics_by_name(&self, name: &str) -> Vec<PerformanceMetric>;
    pub async fn get_metrics_by_type(&self, metric_type: &PerformanceMetricType) -> Vec<PerformanceMetric>;
    pub async fn calculate_average(&self, name: &str) -> f64;
    pub async fn calculate_max(&self, name: &str) -> f64;
    pub async fn calculate_min(&self, name: &str) -> f64;
    pub async fn clear_metrics(&self);
}
```

## 4. 性能指标分析器设计

```text
PerformanceAnalyzer {
    collector: Arc<PerformanceMetricsCollector>,
}

impl PerformanceAnalyzer {
    pub fn new(collector: Arc<PerformanceMetricsCollector>) -> Self;
    pub async fn analyze_performance(&self, name: &str) -> PerformanceAnalysis;
}
```

## 5. 测试套件设计

```text
PerformanceTestSuite {
    tests: Vec<PerformanceTest>,
}

impl PerformanceTestSuite {
    pub fn new_default() -> Self;
    pub async fn run_all(&self) -> Result<Vec<PerformanceTestResult>>;
    async fn run_test(&self, test: &PerformanceTest) -> Result<PerformanceTestResult>;
}
```

## 6. 失败关闭

- 性能指标收集必须支持异步操作。
- 性能指标分析必须支持统计计算。
- 性能指标查询必须支持按名称和类型过滤。
- 所有测试必须在隔离环境运行，不影响生产环境。

## 7. 接入命令

性能指标收集器可通过以下方式使用：

```text
// 创建收集器
let collector = Arc::new(PerformanceMetricsCollector::new());

// 记录性能指标
let mut labels = HashMap::new();
labels.insert("component".to_string(), "system".to_string());
collector.record_metric(
    "response_time",
    PerformanceMetricType::ResponseTime,
    100.0,
    "ms",
    labels,
).await;

// 分析性能指标
let analyzer = PerformanceAnalyzer::new(collector);
let analysis = analyzer.analyze_performance("response_time").await;
```

## 8. 发布限制

性能指标收集模块只用于隔离环境验证，不影响生产环境配置和运行。真实性能指标收集需要独立的需求和审批流程。

## 9. 门禁记录

| 门禁 | 结果 | 日期 |
|---|---|---|
| 需求确认 | 通过（所有者确认，需求文档同记录） | 2026-09-09 |
| 设计评审 | 通过（随发布签收由所有者确认） | 2026-09-09 |
| 实现与验证 | 完成：`cargo test --workspace`（169 项通过）、`cargo clippy --workspace --all-targets -- -D warnings` 零警告、`npm run build` 零错误；性能指标收集测试套件运行成功 | 2026-09-09 |
| 发布签收 | 通过（所有者确认签收，阶段 E 发布闭环） | 2026-09-09 |
